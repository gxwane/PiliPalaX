# Icon sources

These PNG files are build-time sources for platform launcher and splash assets.
They are kept outside Flutter's bundled `assets/` directories so they do not
increase the installed application size.
